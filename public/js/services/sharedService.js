// 'use strict';

// //User service used for REST endpoint
// angular.module('mean.shared').factory('SharedService', ['$rootscope', function($rootscope) {
//     var SharedService = {};

//     SharedService.items = {};

//     SharedService.prepBroadcast = function (items) {
//       this.items = items;
//       this.broadcastItems();
//     };

//     SharedService.broadcastItems = function () {
//       rootscope.$broadcast('itemsBroadcast')
//     }
//   }
//   ]);
